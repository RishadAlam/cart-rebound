# Works across npm, pnpm and yarn.
engine-strict=false

# pnpm: install peer dependencies automatically so the WordPress ESLint
# toolchain resolves without manual peer wrangling.
auto-install-peers=true
