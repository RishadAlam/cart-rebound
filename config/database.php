<?php
/**
 * Database configuration.
 *
 * @package CartRebound
 */

declare( strict_types=1 );

defined( 'ABSPATH' ) || exit;

return array(
	// Table prefix appended to the WordPress table prefix (e.g. wp_cart_rebound_).
	'prefix'  => 'cart_rebound_',
	'charset' => 'utf8mb4',
	'collate' => '',
);
